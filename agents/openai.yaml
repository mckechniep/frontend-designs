interface:
  display_name: "Kickass Frontend"
  short_description: "Planner-first frontend builds and refinements"
  default_prompt: "Use $kickass-frontend to lock task mode, scope, and surface archetype against the canonical profile/runtime references: full builds return PLAN + DESIGN_SPEC, while small refinements use a targeted delta flow."
policy:
  allow_implicit_invocation: false
