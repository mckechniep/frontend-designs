interface:
  display_name: "Kickass Frontend"
  short_description: "Plan-first frontend UI generation with DESIGN_SPEC"
  default_prompt: "Use $kickass-frontend to scope-lock the task, produce PLAN + DESIGN_SPEC, then implement a verified, accessible frontend UI."
policy:
  allow_implicit_invocation: false
