# Landing A/B Notes

- Date:
- Model/version:
- Repo state (commit/tag/snapshot):
- Any extra constraints:
- Desktop viewport:
- Mobile viewport:
- Font delivery mode (A/B/C/D):
- If `B`: network/CSP notes:
- If `C`: local/system fallback stack used:
- If `D`: custom font file paths + weights:

Quality deltas observed:

- With skill:
- Without skill:
